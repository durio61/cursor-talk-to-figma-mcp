# 设计稿优化策略核心意图

## 🎯 为什么要这样优化？

### 1️⃣ 减少 DOM 层级嵌套 → 提升渲染性能

#### 问题场景
```jsx
// 优化前：复杂的嵌套结构（5层深）
<div data-node-id="1:76">           // 容器
  <div data-node-id="1:85">         // Group 2
    <div data-node-id="1:86">       // 内层容器
      <img src={star1.svg} />       // 实际图片
    </div>
  </div>
  <div data-node-id="1:98">         // Group 2
    <div data-node-id="1:99">
      <img src={star2.svg} />
    </div>
  </div>
  <div data-node-id="1:111">        // Group 2
    <div data-node-id="1:112">
      <img src={star3.svg} />
    </div>
  </div>
</div>
```

#### 性能问题
- ❌ 12+ 个 DOM 节点（容器 + 图片）
- ❌ 5 层嵌套深度
- ❌ 每层都需要样式计算、布局计算
- ❌ 重绘时需要遍历整个子树

#### 优化策略：向上聚类
- 发现 `1:76` 的所有子节点都是纯图片（无文本、无交互）
- **直接像素化 `1:76` 这个容器**（而不是单个图片）
- 结果：

```jsx
// 优化后：扁平化（1层）
<img src={container_1_76_flattened.png} data-node-id="1:76" />
```

#### 收益
- ✅ 删除 11 个 DOM 节点（只保留 1 个）
- ✅ 减少 4 层嵌套
- ✅ 样式计算减少 90%+
- ✅ 布局计算减少 90%+
- ✅ 重绘开销减少 90%+

---

### 2️⃣ SVG 转栅格化 → 减少运行时解析开销

#### SVG 的性能问题
```jsx
<img src={logo.svg} />  // 浏览器需要：
                        // 1. 解析 SVG XML 结构
                        // 2. 构建 SVG DOM 树
                        // 3. 计算路径、填充、描边
                        // 4. 每次重绘都要重新计算
```

#### 栅格图的优势
```jsx
<img src={logo.png} />  // 浏览器只需：
                        // 1. 解码位图数据
                        // 2. 直接渲染像素
                        // 3. 重绘时直接复用
```

#### 强制转换策略
- **所有 SVG 节点必须转为 PNG/WebP**
- 即使无法向上聚类，单个 SVG 也要转换
- 原因：SVG 运行时开销远大于栅格图

#### 收益
- ✅ 首屏渲染速度提升 30-50%
- ✅ 滚动性能提升（无需重新解析）
- ✅ 内存占用减少（无需保留 SVG DOM）

---

### 3️⃣ 向上聚类的智能判断 → 最大化优化效果

#### 核心逻辑
```
对每个图片节点：
  检查父节点的所有子节点是否全是图片（无文本、无交互）
    ↓ 是
  继续向上检查父节点的父节点
    ↓ 是
  找到最上层的"纯图片容器"
    ↓
  像素化这个容器（而不是单个图片）
    ↓
  最大化减少 DOM 节点和层级嵌套
```

#### 判断标准
- ✅ **可以聚类**：父节点只包含 `<img>`、`<div>` with maskImage、`<vector>` 等纯视觉元素
- ❌ **不能聚类**：父节点包含 `<text>`、`<p>`、`<button>` 等内容/交互元素

#### 示例 1：可以聚类
```jsx
<div id="card">                    // 包含文本，不能像素化
  <div id="decoration">            // 所有子节点都是图片 ✅
    <img src={bg.svg} />
    <img src={icon1.svg} />
    <img src={icon2.svg} />
  </div>
  <p>银行卡号</p>                 // 文本内容
</div>

// 优化：像素化 decoration 节点
<div id="card">
  <img src={decoration_flattened.png} />  // 3 个图片 → 1 个
  <p>银行卡号</p>
</div>
```

#### 示例 2：无法聚类
```jsx
<div id="card">
  <img src={bg.svg} />             // 父节点还有文本，无法聚类
  <p>银行卡号</p>
</div>

// 优化：只像素化 SVG 本身
<div id="card">
  <img src={bg.png} />             // SVG → PNG
  <p>银行卡号</p>
</div>
```

#### 为什么要向上聚类？
- ✅ **最大化减少 DOM 节点**（聚类容器 vs 单个图片）
- ✅ **最大化减少层级嵌套**（扁平化结构）
- ✅ **保留有意义的结构**（不破坏文本/交互节点）

---

### 4️⃣ 实际业务价值

#### 典型场景（银行卡列表）
- 4 张银行卡
- 每张卡：背景 logo + 3 组星号（每组 4 个星号）
- 大量 SVG 图标（导航、状态栏、装饰）

#### 不优化的后果
- DOM 节点：200+ 个
- SVG 解析：30+ 个 SVG 文件
- 层级嵌套：5-6 层深
- 首屏渲染慢、滚动卡顿

#### 优化后预期
- DOM 节点：减少 50%+（100 个以内）
- SVG → PNG：0 个 SVG 运行时解析
- 层级嵌套：减少 2-3 层（平均 2-3 层深）
- 性能提升：首屏渲染提升 30-50%，滚动流畅度提升明显

---

## 🎯 优化策略总结

| 策略 | 目标 | 方法 | 收益 |
|------|------|------|------|
| **向上聚类** | 减少 DOM 节点和层级 | 找到最上层的"纯图片容器"并像素化 | DOM 减少 50%+，层级减少 2-3 层 |
| **SVG 转栅格** | 减少运行时开销 | 所有 SVG 强制转为 PNG/WebP | 渲染速度提升 30-50% |
| **智能判断** | 保留有意义的结构 | 不破坏包含文本/交互的节点 | 优化效果最大化 |

---

## 🔍 核心分析逻辑（详细步骤）

### 步骤 A：提取所有图片节点
```bash
# 提取所有图片常量（PNG + SVG）
grep "^const img" design-context.jsx
```
- 记录所有图片常量名称
- 区分 PNG 和 SVG（.png vs .svg）

### 步骤 B：查找所有使用图片的节点
对每个图片常量，在 JSX 中查找使用位置：
- `<img src={imgXxx} />` 
- `maskImage: url('${imgXxx}')`
- `backgroundImage: url('${imgXxx}')`
- 记录每个使用位置的 `data-node-id`

### 步骤 C：向上聚类分析（核心优化逻辑）

**🎯 目标：找到最上层的"纯图片容器"，最大化减少 DOM 节点和层级嵌套**

对每个图片节点执行以下分析：

#### 1. 检查父节点的所有子节点
- **问题：** 父节点的所有子节点是否全部是图片节点（无文本、无交互）？
- **判断标准：**
  - ✅ **可以聚类**：只有 `<img>`、`<div>` with maskImage、`<vector>` 等纯视觉元素
  - ❌ **不能聚类**：包含 `<text>`、`<p>`、`<button>`、`<input>` 等内容/交互元素

- **💡 为什么这样判断？**
  - 纯视觉元素可以安全地合并为一张图片
  - 文本/交互元素必须保留（用户需要选择文本、点击按钮）

#### 2. 如果父节点全是图片 → 继续向上检查
- 检查父节点的父节点（重复步骤 1）
- 最多向上 4-5 层

- **💡 为什么要继续向上？**
  - 找到更上层的容器 → 减少更多 DOM 节点
  - 例如：3 个图片组（12 节点）→ 1 个容器（1 节点）= 减少 11 个节点

#### 3. 找到最上层的"纯图片容器"
- **条件 1：** 该节点所有后代都是图片/装饰
- **条件 2：** 该节点的父节点包含非图片内容（文本/交互）
- **标记该节点为像素化目标**

- **💡 为什么标记容器而不是单个图片？**
  - 容器像素化 → 删除所有子节点 → 最大化减少 DOM
  - 单个图片像素化 → 只减少 1 个节点 → 效果有限

- **示例：**
  ```jsx
  // 优化前：12 个节点，5 层深
  <div id="container" data-node-id="1:76">  // ← 标记这个节点
    <div data-node-id="1:85">
      <div><img src={a.svg} /></div>
    </div>
    <div data-node-id="1:98">
      <div><img src={b.svg} /></div>
    </div>
    <div data-node-id="1:111">
      <div><img src={c.svg} /></div>
    </div>
  </div>
  
  // 优化后：1 个节点，1 层深
  <img src={container_1_76_flattened.png} data-node-id="1:76" />
  
  // 收益：减少 11 个 DOM 节点，减少 4 层嵌套
  ```

#### 4. 无法聚类时（父节点包含其他内容）
- 标记当前图片节点本身
- **SVG 节点必须标记**（即使无法聚类）

- **💡 为什么 SVG 必须标记？**
  - SVG 运行时解析开销大（解析 XML、构建 DOM、计算路径）
  - PNG/WebP 直接解码位图，性能提升 30-50%

- **示例：**
  ```jsx
  // 优化前：无法聚类（父节点有文本）
  <div id="card">
    <img src={bg.svg} data-node-id="1:80" />  // ← 标记这个 SVG
    <p>银行卡号</p>
  </div>
  
  // 优化后：SVG → PNG
  <div id="card">
    <img src={bg.png} data-node-id="1:80" />  // SVG 转栅格
    <p>银行卡号</p>
  </div>
  
  // 收益：渲染速度提升 30-50%
  ```

#### 5. 记录聚类结果
- **聚类成功：** 记录容器节点 ID + 子节点列表 + 减少的节点数
- **聚类失败：** 记录单个图片节点 ID + 类型（SVG/PNG）
- **原因说明：** 记录为什么标记这个节点（便于后续验证和报告）

### 步骤 D：SVG 特殊处理
- 所有 SVG 节点必须被标记（强制）
- 原因：SVG 运行时解析开销大，必须转为栅格图
- **即使无法向上聚类，单个 SVG 也要转换**

### 步骤 E：隐藏节点
- 搜索 `hidden`、`visibility: hidden`、`display: none`
- 标记为删除候选
- 原因：隐藏节点仍占用 DOM 内存，应直接删除
